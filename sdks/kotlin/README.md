# Orion Kotlin SDK

Kotlin/JVM SDK backed by an in-process JNI library. It provides suspending model
and tool APIs, `Flow` event delivery, a model registry, and an OpenAI-compatible
adapter.

```bash
cd sdks/kotlin && ./gradlew test
```
